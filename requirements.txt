# GPR-FDA 1.0.0 runtime dependencies
# Python 3.10 or newer is required by the source syntax.
# Install this file with: python -m pip install -r requirements.txt

# Numerical arrays, linear algebra, and random sampling
numpy>=1.24

# Tabular data loading, transformation, and CSV export
pandas>=1.5

# Optimization, interpolation, statistics, signal processing, and matrix tools
scipy>=1.10

# Gaussian-process regression, kernels, scaling, and validation metrics
scikit-learn>=1.3

# Publication plots and diagnostic visualizations
matplotlib>=3.7
